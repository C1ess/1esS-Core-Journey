### EP.66 🟣 AI INSIDE — 키포인트

* **AI도 새로운 학습 과정에서 기존 지식을 잊을 수 있다**
* **가중치(Weight) 조정 → 기존 학습 내용에 영향**
* **치명적 망각(Catastrophic Forgetting) → 이전 지식 유지에 실패하는 현상**
* **새로운 데이터 집중 학습 → 기존 능력 저하 가능성**
* **AI 지식 → 별도 파일이 아닌 모델 가중치에 분산되어 저장**
* **망각 방지 방법 → 기존 데이터 재학습·학습률 조절·파라미터 보호**
* **AI 학습 → GPU와 메모리 시스템의 연산·데이터 처리 필요**
* **AI의 핵심 과제 → 새로운 지식 학습과 기존 지식 유지의 균형**
